# TimePicker

Horas/minutos com `min`, `max`, `step` e limpar.
```razor
<orizon-time-picker asp-for="StartTime" label="Horário" step="900" />
```
Use `step` em segundos e valide intervalos no modelo.
