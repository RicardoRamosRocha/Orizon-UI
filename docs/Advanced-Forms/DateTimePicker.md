# DateTimePicker

Combina data e hora via `datetime-local`, com `min`, `max` e limpar.
```razor
<orizon-date-time-picker asp-for="PublishedAt" label="Publicação" />
```
Não inclui fuso horário; converta explicitamente no servidor quando necessário.
