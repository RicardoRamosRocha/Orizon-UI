# TextArea

Texto multilinha com `rows`, `maxlength`, `counter` e `auto-resize`, além da API comum.
```razor
<orizon-textarea asp-for="Notes" label="Observações" rows="4" maxlength="500" counter />
```
Auto-resize mantém edição confortável; defina limite coerente com a validação do servidor.
