# Select

Select nativo com placeholder, grupos via conteúdo `<optgroup>`, `items`, `multiple` e `searchable` preparado.
```razor
<orizon-select asp-for="CategoryId" items="Model.Categories" placeholder="Selecione..." />
```
O controle nativo preserva teclado e leitores de tela. Use MultiSelect para seleção rica com chips.
