# ProgressRing

Progresso circular com `value`, `min`, `max`, `size`, `thickness`, `label`, `animated`, `status`.
```razor
<orizon-progress-ring value="78" label="Atingido" status="success" />
```
Emite `role=progressbar` e valores ARIA. Não dependa apenas da cor do status.
