# Sparkline

Micrográfico `line`, `bar` ou `area`. API: `type`, `values`, `color`, `label`.
```razor
<orizon-sparkline type="area" values="[12,18,15,24]" label="Tendência crescente" />
```
Informe `label` quando a tendência for relevante; sem ele o SVG é decorativo.
