function treeChildren(item){return item.querySelector(":scope > ul")}
function toggleTree(item,expanded){item.setAttribute("aria-expanded",String(expanded));const children=treeChildren(item);if(children)children.hidden=!expanded;if(!expanded&&document.activeElement&&children?.contains(document.activeElement))item.focus()}
document.addEventListener("click",event=>{const toggle=event.target.closest("[data-tree-toggle]");if(toggle){const item=toggle.closest("[role=treeitem]");toggleTree(item,item.getAttribute("aria-expanded")!=="true");return}const gridToggle=event.target.closest("[data-tree-grid-toggle]");if(gridToggle){const row=gridToggle.closest("[role=row]");const id=row.dataset.nodeId;const expanded=row.getAttribute("aria-expanded")!=="true";row.setAttribute("aria-expanded",String(expanded));gridToggle.closest("[data-orizon-tree-grid]").querySelectorAll(`[data-parent="${id}"]`).forEach(child=>child.hidden=!expanded);return}const item=event.target.closest("[data-orizon-tree] [role=treeitem]");if(item){const root=item.closest("[data-orizon-tree]");if(root.dataset.selection==="single")root.querySelectorAll("[aria-selected=true]").forEach(x=>x.setAttribute("aria-selected","false"));item.setAttribute("aria-selected","true");item.focus()}const row=event.target.closest("[data-orizon-tree-grid] [role=row]");if(row&&row.closest("[data-orizon-tree-grid]").dataset.selectable==="true"){row.closest("[data-orizon-tree-grid]").querySelectorAll("[aria-selected=true]").forEach(x=>x.setAttribute("aria-selected","false"));row.setAttribute("aria-selected","true")}});
document.addEventListener("keydown",event=>{const item=event.target.closest("[data-orizon-tree] [role=treeitem]");if(!item)return;const root=item.closest("[data-orizon-tree]");const visible=[...root.querySelectorAll("[role=treeitem]")].filter(x=>x.offsetParent!==null);const index=visible.indexOf(item);if(event.key==="ArrowDown"||event.key==="ArrowUp"){event.preventDefault();visible[Math.max(0,Math.min(visible.length-1,index+(event.key==="ArrowDown"?1:-1)))]?.focus()}else if(event.key==="ArrowRight"&&item.hasAttribute("aria-expanded")){event.preventDefault();if(item.getAttribute("aria-expanded")!=="true")toggleTree(item,true);else treeChildren(item)?.querySelector("[role=treeitem]")?.focus()}else if(event.key==="ArrowLeft"){event.preventDefault();if(item.getAttribute("aria-expanded")==="true")toggleTree(item,false);else item.parentElement?.closest("[role=treeitem]")?.focus()}else if(event.key==="Home"){event.preventDefault();visible[0]?.focus()}else if(event.key==="End"){event.preventDefault();visible.at(-1)?.focus()}else if(event.key==="Enter"||event.key===" "){event.preventDefault();item.click()}});
